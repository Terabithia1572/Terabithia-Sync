<UserControl x:Class="ScheduledCopyManager.Presentation.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ScheduledCopyManager.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="850">

    <UserControl.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:StatusToTextConverter x:Key="StatusToTextConverter"/>
        <converters:BytesToFormattedSizeConverter x:Key="BytesToFormattedConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Kopyalama Geçmişi" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Gerçekleştirilen kopyalama işlemlerinin detaylı kayıtları" FontSize="13" Foreground="#7F8C8D" Margin="0,3,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="🗑️ Geçmişi Temizle" Command="{Binding ClearHistoryCommand}" 
                    Padding="14,9" Style="{StaticResource ModernDangerButtonStyle}"/>
        </Grid>

        <!-- Search Bar -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍 Arama: " VerticalAlignment="Center" Margin="5,0,10,0" Foreground="#94A3B8" FontWeight="SemiBold"/>
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         BorderThickness="0" VerticalAlignment="Center" FontSize="13" Foreground="#1E293B"/>
            </Grid>
        </Border>

        <!-- History DataGrid -->
        <Border Grid.Row="4" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="10">
            <Grid>
                <DataGrid ItemsSource="{Binding HistoryEntries}" AutoGenerateColumns="False" IsReadOnly="True"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Görev Adı" Binding="{Binding JobName}" Width="170"/>
                        <DataGridTextColumn Header="Tarih / Saat" Binding="{Binding StartTime, StringFormat=\{0:dd.MM.yyyy HH:mm:ss\}}" Width="150"/>
                        <DataGridTextColumn Header="Süre" Binding="{Binding Duration, StringFormat=\{0:mm\\:ss\}}" Width="80"/>
                        <DataGridTextColumn Header="Kopyalanan" Binding="{Binding FilesCopied}" Width="90"/>
                        <DataGridTextColumn Header="Atlanan" Binding="{Binding FilesSkipped}" Width="80"/>
                        <DataGridTextColumn Header="Hatalı" Binding="{Binding FilesFailed}" Width="80"/>
                        <DataGridTextColumn Header="Aktarılan Veri" Binding="{Binding BytesCopied, Converter={StaticResource BytesToFormattedConverter}}" Width="110"/>
                        <DataGridTemplateColumn Header="Durum" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="6,2" CornerRadius="4" HorizontalAlignment="Left"
                                            Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}">
                                        <TextBlock Text="{Binding Status, Converter={StaticResource StatusToTextConverter}}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="İşlem" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🔍 Ayrıntılar" Padding="8,4" Style="{StaticResource ModernSecondaryButtonStyle}"
                                            Command="{Binding DataContext.ShowDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="Henüz kopyalama geçmişi bulunmuyor."
                           Foreground="#64748B" FontSize="14" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasHistoryEntries}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
