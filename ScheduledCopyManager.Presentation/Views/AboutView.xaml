<UserControl x:Class="ScheduledCopyManager.Presentation.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="850">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Hakkında" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>

        <!-- About Cards Content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,0">
                <!-- Branding Main Card -->
                <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="25" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Width="72" Height="72" VerticalAlignment="Center" Margin="0,0,20,0"
                               Source="pack://application:,,,/ScheduledCopyManager.Presentation;component/Resources/app_icon.ico"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               UseLayoutRounding="True" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding AppName}" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
                            <TextBlock Text="{Binding Description}" FontSize="14" Foreground="#3B82F6" FontWeight="SemiBold" Margin="0,3,0,0"/>
                            <TextBlock Text="{Binding Copyright}" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Technical & Publisher Details -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Publisher Card -->
                    <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="18">
                        <StackPanel>
                            <TextBlock Text="Geliştirici &amp; Yayıncı" FontSize="15" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                            
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Geliştirici:" Foreground="#64748B" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding Publisher}" Foreground="#1E293B" FontWeight="Bold"/>
                            </Grid>

                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Sürüm:" Foreground="#64748B" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding Version}" Foreground="#10B981" FontWeight="Bold"/>
                            </Grid>

                            <TextBlock Text="Bu yazılım Yunus İNAN tarafından geliştirilmiştir." FontSize="11" Foreground="#94A3B8" Margin="0,10,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <!-- Technical Card -->
                    <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="18">
                        <StackPanel>
                            <TextBlock Text="Teknik Bilgiler" FontSize="15" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>

                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Platform:" Foreground="#64748B" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding Platform}" Foreground="#334155"/>
                            </Grid>

                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Çalışma Zamanı:" Foreground="#64748B" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding Runtime}" Foreground="#334155"/>
                            </Grid>

                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Uygulama Tipi:" Foreground="#64748B" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding ApplicationType}" Foreground="#334155"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Features Card -->
                <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Öne Çıkan Özellikler" FontSize="15" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                        <ItemsControl ItemsSource="{Binding Features}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <TextBlock Text="✓  " Foreground="#10B981" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding}" Foreground="#334155" FontSize="13"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
