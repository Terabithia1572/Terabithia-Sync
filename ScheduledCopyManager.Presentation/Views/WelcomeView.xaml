<UserControl x:Class="ScheduledCopyManager.Presentation.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">

    <Grid Background="#70000000">
        <Border Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="12" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">
            <StackPanel>
                <TextBlock Text="Terabithia Sync'ye Hoş Geldiniz!" FontSize="24" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
                <TextBlock Text="Zamanlı Dosya ve Klasör Kopyalama Yöneticisi" FontSize="14" Foreground="#2563EB" FontWeight="SemiBold" Margin="0,4,0,20" HorizontalAlignment="Center"/>

                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="🚀 Ne İşe Yarar?" FontWeight="Bold" FontSize="14" Foreground="#334155" Margin="0,0,0,5"/>
                        <TextBlock Text="Terabithia Sync, bilgisayarınızdaki veya ağınızdaki önemli dosya ve klasörleri belirttiğiniz zaman aralıklarında otomatik olarak istediğiniz konuma veya USB sürücülere kopyalar."
                                   TextWrapping="Wrap" Foreground="#475569" FontSize="13" Margin="0,0,0,12"/>

                        <TextBlock Text="💡 İlk Görevinizi Nasıl Oluşturursunuz?" FontWeight="Bold" FontSize="14" Foreground="#334155" Margin="0,0,0,5"/>
                        <TextBlock Text="1. Sol menüden 'Kopyalama Görevleri' sekmesine geçin.&#10;2. 'Yeni Görev Ekle' butonuna tıklayın.&#10;3. Kopyalanacak dosyaları/klasörleri ve hedef konumu seçin.&#10;4. Çalışma saatini (Günlük, Haftalık vb.) ve kopyalama modunu belirleyip kaydedin."
                                   TextWrapping="Wrap" Foreground="#475569" FontSize="13"/>
                    </StackPanel>
                </Border>

                <Button Content="Başlayın!" Command="{Binding GetStartedCommand}" Padding="20,10" Background="#10B981" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" HorizontalAlignment="Center" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
